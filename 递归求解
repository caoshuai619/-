//递归求解，每当有一个人出圈时，出圈人的下一位就成了新的环的头部，相当于把数据向前移动m位。
//若已知f(n-1,m)--胜利者的下标，则n个人，就是往后移动了n位，考虑越界问题，超过的部分要接到头上，则模n

//所以递推关系式  f(n,m)=(f(n-1,m)+m-1)%n+1     m-1表示起始位置与目标位置的间隔，模N加一表示，出圈人的下一位新的环头

int YuSeFU(int n,int m) {
	if (n <= 1 || m < 1) return -1;
	if (n == 2) {
		if (n%m == 0) return 1;
		else return 2;
	}
	return (YuSeFU(n - 1, m)+m-1) % n + 1;
}
